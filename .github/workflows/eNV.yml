name: Create Environment

on:
  workflow_dispatch:
    inputs:
      environment_name for Dev:
        description: Environment display name
        required: true
        type: string 
           
      security_group_id:
        required: true
        description: Entra ID Security Group Object ID (GUID)
 
      region:
        description: Asxower Platform region
        required: true
        type: string
        
      type:
        description: 'The type of environment (Trial, Sandbox, Production, SubscriptionBasedTrial).'
        required: true
        default: 'Sandbox'
        
      user:
        description: 'Object ID or user principal name (UPN) of Microsoft Entra ID user to be assigned to the environment.'
        required: false
    
      currency:
        description: 'The currency to use for the environment.'
        required: false
        default: 'USD'
    
      language:
        description: 'Sets the language used for your environment. Defaults to "English (United States)".'
        required: false
        default: 'English (United States)'
    
      security-group-id:
        description: 'Microsoft Entra ID Security Group ID to use when creating the environment.'
        required: false

jobs:
  create: 
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Power Platform Tools
        uses: microsoft/powerplatform-actions/actions-install@v1

      - name: Create Environment
        uses: microsoft/powerplatform-actions/create-environment@v1
        with:
          name: ${{ inputs.environment_name }}        # ✅ REQUIRED
          region: ${{ inputs.region }}                # ✅ REQUIRED
          tenant-id: ${{ secrets.PP_TENANT_ID }}
          app-id: ${{ secrets.PP_APP_ID }}
          client-secret: ${{ secrets.PASSWORD }}
